# Ambient Wildlife Monitoring API Configuration

# API Settings
APP_NAME="Ambient Wildlife Monitoring API"
APP_VERSION="1.0.0"
DEBUG=false

# Server Settings
HOST=0.0.0.0
PORT=8000

# Paths (relative to project root or absolute)
CLASSIFIER_MODEL_PATH=classifier-models/train-fox-background/weights/best.pt
VIDEO_ASSETS_DIR=video-assets/normal
OUTPUT_DIR=output

# VLM Settings (optional - leave empty for mock verification)
VLM_ENDPOINT=

# CORS Settings (comma-separated origins, or * for all)
CORS_ORIGINS=*
